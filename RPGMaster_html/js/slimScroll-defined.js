	$( document ).ready(function(){
	  $("#character-info").slimScroll({
		  height: '84%'
	  });
	  $("#party-info").slimScroll({
		  height: 'auto',
		  railVisible: false,
	  });
	  $("#chat").slimScroll({
		  height: 'auto'
	  });
	});